# secrets / local config
.env

# python
__pycache__/
*.pyc
.venv/
env/

# model weights / HF cache should never be committed
*.safetensors
*.bin
hf_cache/

# control-plane runtime state (per-machine; regenerated on first run)
models.local.json
.run/

# runtime logs
*.log

# llama.cpp build (from setup_llamacpp.sh)
.llamacpp/

# transformers shim venv (from setup_shim.sh; per-machine, often a symlink)
.shim-venv
